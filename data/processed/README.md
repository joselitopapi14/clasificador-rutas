# Datos Procesados (Processed Data)

Esta carpeta contiene la referencia a los datos después de la limpieza y unificación de clases (Fase 1). 

Por restricciones de tamaño de GitHub, el archivo procesado no está aquí.

**Ubicación de trabajo:**
El archivo consolidado `dataset_clean_final.zip` (que contiene más de 200,000 imágenes organizadas con el formato `Producto_Estado`) se encuentra almacenado en Google Drive bajo la ruta `/Proyecto_ML_Frescura/data/processed/`.
